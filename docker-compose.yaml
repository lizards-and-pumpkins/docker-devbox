version: "2"

services:
  lizards:
    image: lizardsandpumpkins/devbox:0.3
    environment:
      BASE_URL: 127.0.0.1
      PORT: 8080
    ports:
      - 8080:80
    volumes:
      - /var/www/lizards
